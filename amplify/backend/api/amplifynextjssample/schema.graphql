type Post
  @model
{
  id: ID!
  content: String!
  owner: String!
  image: String
  status: PostStatus! @index(name: "SortByCreatedAt", sortKeyFields: ["createdAt"])
  comments: [Comment] @hasMany(fields: ["id"])
  createdAt: AWSDateTime
}

enum PostStatus {
  published
  unpublished
}

type Comment
  @model
{
  id: ID!
  postId: ID! @index(name: "byPost", sortKeyFields: ["content"])
  content: String!
  owner: String!
  post: Post @belongsTo(fields: ["postId"])
}
